# Database and runtime outputs
tx_lottery.db
tx_lottery_latest.json
tx_lottery.log

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Node
ui/node_modules/
ui/dist/

# Env / secrets
.env
*.env

# Claude Code local project data
# .claude/ replaced by the scoped block below; a bare directory ignore cannot be pierced by negations

# OS
.DS_Store
Thumbs.db

# Claude Code working files; machinery ships, per-project markers stay ignored
.claude/*
!.claude/agents/
!.claude/skills/
!.claude/hooks/
!.claude/settings.json
!.claude/risk-paths.json

# Active bar marker stamped by /bar, read by the bar gate hook
.current-bar

# Active tier marker stamped by /route, read by the risk gate hook
.current-route

# Adjudicator CONFIRM stamp from /verify-up, read by the commit gate
.adjudication-pass
